# API Versioning

## Strategy
URL-based versioning: `/api/v1/`, `/api/v2/`, etc.

## Rules
- Breaking changes require a new version
- Old versions maintained for a deprecation period
- Version is explicit in the URL path
