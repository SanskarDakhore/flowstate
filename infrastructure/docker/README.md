# Docker

Docker configurations for FLOWSTATE.

## Local Development
A Docker Compose setup for running PostgreSQL and the backend locally will be added when the database tooling is confirmed.

## Production
Production Docker images TBD — depends on cloud provider selection.
