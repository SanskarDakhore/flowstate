# PlayMode Tests

Unity PlayMode tests for FLOWSTATE.

Tests that run in Play mode — required for:
- MonoBehaviour lifecycle testing
- Coroutine testing
- Scene loading
- Integration testing with Unity systems

Place test files here with the naming convention: `*Tests.cs`
