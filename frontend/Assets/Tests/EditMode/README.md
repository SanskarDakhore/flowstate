# EditMode Tests

Unity EditMode tests for FLOWSTATE.

Tests that run without entering Play mode — ideal for:
- Pure C# logic (scoring, state machine, events)
- ScriptableObject validation
- Editor tool testing

Place test files here with the naming convention: `*Tests.cs`
