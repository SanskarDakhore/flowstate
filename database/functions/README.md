# Database Functions

This directory contains SQL database functions, triggers, and stored procedures.

## Guidelines
- Use for database-level routines, like updating timestamps or auto-incrementing rankings on score submission.
