# Database Queries

This directory contains reusable, optimized SQL query templates and statements.

## Guidelines
- Store complex query templates (e.g. paginated leaderboard queries, complex user stats) here.
- Keeping queries isolated helps with performance reviews and DB performance optimization.
