// =============================================================================
// FLOWSTATE — Shared Enums
// =============================================================================

export enum GameMode {
  Classic = 'classic',
  Endless = 'endless',
  Challenge = 'challenge',
  Daily = 'daily',
}

export enum SessionStatus {
  Active = 'active',
  Completed = 'completed',
  Abandoned = 'abandoned',
}

export enum LeaderboardPeriod {
  AllTime = 'all-time',
  Weekly = 'weekly',
  Daily = 'daily',
}
